---
// Versión EN de /comparar/facturacion. La plantilla resuelve el idioma desde la
// URL (getLangFromUrl), así que no necesita props — igual que /en/precios.
export const prerender = true;
import ComparaTemplate from '../../comparar/facturacion.astro';
---

<ComparaTemplate />
