---
// /roadmap — Product detailed roadmap, inspired by Stripe's roadmap
export const prerender = true;

import Layout from '../layouts/Layout.astro';
import Nav from '../components/landing/Nav.astro';
import Footer from '../components/landing/Footer.astro';
import PageAnims from '../components/landing/PageAnims.astro';
import RoadmapCoverBg from '../components/landing/RoadmapCoverBg.jsx';
import { roadmapData } from '../lib/roadmap-data';

const statusMap = {
    'live': 'Disponible',
    'beta': 'En beta',
    'next': 'Próximamente'
};

// BreadcrumbList (jul 2026, auditoría SEO/AI-SEO)
const breadcrumbSchema = {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Cord', item: 'https://cord.flouvia.com/' },
        { '@type': 'ListItem', position: 2, name: 'Roadmap', item: 'https://cord.flouvia.com/roadmap' },
    ],
};
---

<Layout
    title="Roadmap y Plataforma — Cord"
    description="Descubre todas las herramientas y funciones de Cord a detalle. Desde el editor de cotizaciones hasta facturación y cobranza con IA."
>
    <script slot="head" type="application/ld+json" is:inline set:html={JSON.stringify(breadcrumbSchema)} />
    <Nav />
    <main class="rd-main">
        <!-- ── HERO ── -->
        <header class="rd-hero">
            <div class="rd-hero-bg" aria-hidden="true">
                <RoadmapCoverBg client:only="react" />
            </div>
            <div class="rd-hero-inner">
                <p class="pp-eyebrow reveal">PLATAFORMA CORD</p>
                <h1 class="rd-h1 masked-title">Todo lo que estamos construyendo.</h1>
                <p class="rd-lead reveal">
                    Cord es el sistema operativo para ventas B2B. Conoce a detalle cada módulo de nuestra plataforma, diseñado para que cierres más tratos y cobres más rápido, sin fricción.
                </p>
            </div>
        </header>

        <!-- ── CONTENIDO ROADMAP ── -->
        <div class="rd-layout">
            <!-- Sidebar: Filters -->
            <aside class="rd-sidebar reveal">
                <div class="rd-sidebar-card">
                    <div class="rd-tabs" role="tablist">
                        <button class="rd-tab is-active" data-tab-target="all" role="tab">Plan de desarrollo</button>
                        <button class="rd-tab" data-tab-target="live" role="tab">Lanzados</button>
                    </div>
                    
                    <div class="rd-filters">
                    <p class="rd-filters-title">Filtrar por</p>
                    
                    <div class="rd-filter-group">
                        <select class="rd-select" id="filter-area" aria-label="Área de producto">
                            <option value="all">Área de producto</option>
                            <option value="cotizaciones">Cotizaciones y Ventas</option>
                            <option value="finanzas">Finanzas y Cobranza</option>
                            <option value="fiscal">Fiscal y Cumplimiento</option>
                        </select>
                        <i class="rd-select-ico"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg></i>
                    </div>

                    <div class="rd-filter-group">
                        <select class="rd-select" id="filter-status" aria-label="Disponibilidad">
                            <option value="all">Disponibilidad</option>
                            <option value="live">Disponible</option>
                            <option value="beta">En beta</option>
                            <option value="next">Próximamente</option>
                        </select>
                        <i class="rd-select-ico"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg></i>
                    </div>

                    <label class="rd-toggle-wrap">
                        <span class="rd-toggle-label">Mostrar solo integrables (API)</span>
                        <input type="checkbox" class="rd-toggle-input" id="filter-api" />
                        <span class="rd-toggle-slider"></span>
                    </label>
                </div>
                </div>
            </aside>

            <!-- Lista de Productos (Rows) -->
            <div class="rd-content">
                <div class="rd-list" id="roadmap-list">
                    {roadmapData.map(item => (
                        <a 
                            href={`/roadmap/${item.slug}`} 
                            class="rd-row reveal" 
                            data-area={item.area} 
                            data-status={item.status} 
                            data-api={String(item.api)}
                        >
                            <div class={`rd-row-status status-${item.status}`}>{statusMap[item.status]}</div>
                            <div class="rd-row-main">
                                <div class="rd-row-copy">
                                    <h3>{item.title.es}</h3>
                                    <p>{item.shortDesc.es}</p>
                                </div>
                                <div class="rd-row-arrow">
                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
                                </div>
                            </div>
                        </a>
                    ))}

                    <!-- Empty state para filtros -->
                    <div id="rd-empty" class="rd-empty" style="display: none;">
                        <p>No hay productos que coincidan con los filtros seleccionados.</p>
                        <button id="rd-clear-filters" class="rd-btn-ghost">Limpiar filtros</button>
                    </div>

                </div>
            </div>
        </div>

    </main>
    <Footer />
    <PageAnims />
</Layout>

<style>
    /* Estilos base (Mobile First) */
    .rd-main {
        background: var(--color-bg);
        min-height: 100vh;
    }

    .pp-eyebrow { font-size: 0.65rem; font-weight: 800; letter-spacing: 3px; text-transform: uppercase; color: rgba(210, 228, 255, 0.75); margin: 0 0 1rem; }
    @media (min-width: 900px) { .pp-eyebrow { margin: 0 0 1.4rem; } }

    /* Hero — sección oscura full-bleed con el shader de fondo (mismo motor que el hero de soporte) */
    .rd-hero {
        position: relative;
        overflow: hidden;
        text-align: center;
        padding: 7rem 5% 3rem;
    }
    @media (min-width: 900px) {
        .rd-hero { padding: 7.5rem 5% 4rem; }
    }
    .rd-hero-bg {
        position: absolute;
        inset: 0;
        background: #050f22;
        z-index: 0;
    }
    .rd-hero-inner {
        position: relative;
        z-index: 2;
        max-width: 800px;
        margin: 0 auto;
    }
    .rd-h1 {
        font-size: clamp(2.4rem, 8vw, 4.2rem);
        font-weight: 800; line-height: 1.05; letter-spacing: -0.035em;
        color: #ffffff !important; text-shadow: 0 2px 24px rgba(0,0,0,0.35);
        margin: 0 0 1.2rem;
    }
    .rd-h1 :global(.m-line-in) { color: #ffffff !important; }
    @media (min-width: 900px) { .rd-h1 { margin: 0 0 1.4rem; } }
    .rd-lead {
        font-size: clamp(1rem, 4vw, 1.15rem); line-height: 1.6; color: rgba(226, 236, 253, 0.78);
    }

    /* Layout principal */
    .rd-layout {
        display: grid;
        grid-template-columns: 1fr;
        gap: 3rem;
        max-width: 1200px;
        margin: 0 auto;
        padding: 3rem 5% 4rem;
        align-items: start;
    }
    @media (min-width: 900px) {
        .rd-layout { grid-template-columns: 280px 1fr; gap: 5rem; padding: 4.5rem 5% 5rem; }
    }

    /* Sidebar & Filters — panel plano "Apple gray", sin borde ni sombra de card */
    .rd-sidebar {
        position: relative;
        top: 0;
        z-index: 10;
    }
    @media (min-width: 900px) {
        .rd-sidebar { position: sticky; top: 6rem; z-index: 1; }
    }

    .rd-sidebar-card {
        background: var(--color-bg-soft);
        border-radius: 28px;
        padding: 1.6rem;
    }
    @media (min-width: 900px) {
        .rd-sidebar-card { border-radius: 30px; padding: 1.8rem; }
    }

    /* Segmented control estilo iOS — pill deslizante entre 2 tabs */
    .rd-tabs {
        position: relative;
        display: flex;
        gap: 2px;
        padding: 3px;
        margin-bottom: 1.8rem;
        background: rgba(10, 25, 47, 0.05);
        border-radius: 13px;
    }
    .rd-tabs::before {
        content: '';
        position: absolute;
        top: 3px; left: 3px;
        width: calc(50% - 3px);
        height: calc(100% - 6px);
        background: #ffffff;
        border-radius: 10px;
        box-shadow: 0 1px 3px rgba(10,25,47,0.10), 0 1px 1px rgba(10,25,47,0.04);
        transition: transform 0.35s var(--ease-spring);
    }
    .rd-tabs:has(.rd-tab:nth-child(2).is-active)::before {
        transform: translateX(100%);
    }
    .rd-tab {
        position: relative;
        z-index: 1;
        flex: 1;
        background: none;
        border: none;
        padding: 0.6rem 0.8rem;
        border-radius: 10px;
        font-family: inherit;
        font-size: 0.82rem;
        font-weight: 600;
        color: var(--color-text-muted);
        cursor: pointer;
        transition: color 0.3s;
        white-space: nowrap;
    }
    .rd-tab:hover {
        color: var(--color-text);
    }
    .rd-tab.is-active {
        color: var(--color-text);
        font-weight: 700;
    }

    .rd-filters-title {
        font-size: 0.65rem;
        font-weight: 800;
        letter-spacing: 1.6px;
        text-transform: uppercase;
        color: var(--color-text-muted);
        margin: 0 0 1rem;
    }
    .rd-filters {
        display: flex;
        flex-direction: column;
        gap: 0.6rem;
    }

    .rd-filter-group {
        position: relative;
    }
    .rd-select {
        width: 100%;
        appearance: none;
        background: #ffffff;
        border: none;
        border-radius: 13px;
        padding: 0.7rem 2.5rem 0.7rem 1.1rem;
        font-family: inherit;
        font-size: 0.85rem;
        font-weight: 500;
        color: var(--color-text);
        cursor: pointer;
        transition: box-shadow 0.3s ease;
    }
    .rd-select:hover {
        box-shadow: 0 1px 2px rgba(10,25,47,0.06);
    }
    .rd-select:focus {
        outline: none;
        box-shadow: 0 0 0 3px rgba(10,25,47,0.10);
    }
    .rd-select-ico {
        position: absolute;
        right: 1.1rem;
        top: 50%;
        transform: translateY(-50%);
        pointer-events: none;
        color: var(--color-text-muted);
    }
    .rd-select-ico svg { width: 15px; height: 15px; }

    /* Toggle Switch */
    .rd-toggle-wrap {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 0.8rem;
        cursor: pointer;
        margin-top: 1rem;
    }
    .rd-toggle-input {
        display: none;
    }
    .rd-toggle-slider {
        position: relative;
        width: 44px;
        height: 24px;
        background: #D1D1D6;
        border-radius: 100px;
        transition: background 0.3s;
        flex-shrink: 0;
    }
    .rd-toggle-slider::before {
        content: '';
        position: absolute;
        width: 18px;
        height: 18px;
        border-radius: 50%;
        background: #fff;
        top: 3px;
        left: 3px;
        transition: transform 0.3s var(--ease-spring);
    }
    .rd-toggle-input:checked + .rd-toggle-slider {
        background: #34c759;
    }
    .rd-toggle-input:checked + .rd-toggle-slider::before {
        transform: translateX(20px);
    }
    .rd-toggle-label {
        font-size: 0.9rem;
        color: var(--color-text);
        line-height: 1.4;
    }

    /* List & Rows */
    .rd-list {
        display: flex;
        flex-direction: column;
    }

    .rd-row {
        display: flex;
        flex-direction: column;
        padding: 2rem 0;
        margin: 0;
        border-top: 1px solid var(--color-border);
        cursor: pointer;
        text-decoration: none;
        border-radius: 0;
        transition: all 0.4s var(--ease-spring);
    }
    @media (min-width: 900px) {
        .rd-row { padding: 2rem 1.5rem; margin: -1rem -1.5rem; border-radius: 12px; }
        .rd-row:hover { background: var(--color-bg-soft); transform: scale(0.995); border-top-color: transparent; }
        .rd-row:hover + .rd-row { border-top-color: transparent; }
        .rd-row:hover .rd-row-arrow { transform: translateX(4px); color: var(--color-text); }
    }
    .rd-row:first-child {
        border-top-color: transparent;
    }

    /* Status Badges */
    .rd-row-status {
        display: inline-block;
        font-size: 0.65rem;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 0.8rem;
    }
    .status-live { color: #059669; }
    .status-beta { color: #d97706; }
    .status-next { color: #4f46e5; }

    .rd-row-main {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 1.5rem;
    }
    @media (min-width: 900px) { .rd-row-main { gap: 2rem; } }

    .rd-row-copy h3 {
        font-size: 1.2rem;
        font-weight: 700;
        margin: 0 0 0.5rem;
        letter-spacing: -0.01em;
        color: var(--color-text);
        transition: color 0.3s;
    }
    @media (min-width: 900px) { .rd-row-copy h3 { font-size: 1.3rem; } }
    
    .rd-row-copy p {
        font-size: 0.95rem;
        line-height: 1.6;
        color: var(--color-text-muted);
        margin: 0;
        max-width: 650px;
    }
    @media (min-width: 900px) { .rd-row-copy p { font-size: 1rem; } }

    .rd-row-arrow {
        flex-shrink: 0;
        color: var(--color-text-muted);
        transition: transform 0.4s var(--ease-spring), color 0.3s;
        margin-top: 0.2rem;
    }
    .rd-row-arrow svg { width: 20px; height: 20px; }
    @media (min-width: 900px) { .rd-row-arrow svg { width: 22px; height: 22px; } }

    .rd-empty {
        text-align: center;
        padding: 3rem 0;
        color: var(--color-text-muted);
    }
    @media (min-width: 900px) { .rd-empty { padding: 4rem 0; } }
    .rd-empty p { margin-bottom: 1rem; }
    .rd-btn-ghost {
        background: none;
        border: 1px solid var(--color-border);
        padding: 8px 16px;
        border-radius: 8px;
        font-family: inherit;
        font-size: 0.9rem;
        color: var(--color-text);
        cursor: pointer;
    }
    .rd-btn-ghost:hover {
        background: var(--color-bg-soft);
    }

    /* Dark mode support */
    @media (prefers-color-scheme: dark) {
        .rd-sidebar-card {
            background: rgba(255,255,255,0.04);
        }
        .rd-tabs {
            background: rgba(255,255,255,0.05);
        }
        .rd-tabs::before {
            background: rgba(255,255,255,0.10);
            box-shadow: none;
        }
        .rd-select {
            background: rgba(255,255,255,0.07);
        }
        .status-live { color: #34d399; }
        .status-beta { color: #fbbf24; }
        .status-next { color: #818cf8; }
        .rd-toggle-slider { background: #39393D; }
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const selectArea = document.getElementById('filter-area') as HTMLSelectElement;
        const selectStatus = document.getElementById('filter-status') as HTMLSelectElement;
        const toggleApi = document.getElementById('filter-api') as HTMLInputElement;
        const rows = document.querySelectorAll('.rd-row');
        const emptyState = document.getElementById('rd-empty');
        const btnClear = document.getElementById('rd-clear-filters');
        const tabs = document.querySelectorAll('.rd-tab');

        let currentTabFilter = 'all';

        const filterRows = () => {
            const area = selectArea?.value;
            const status = selectStatus?.value;
            const needsApi = toggleApi?.checked;
            let visibleCount = 0;

            rows.forEach((row) => {
                const el = row as HTMLElement;
                const rArea = el.dataset.area;
                const rStatus = el.dataset.status;
                const rApi = el.dataset.api === 'true';

                let show = true;

                // Aplicar el filtro de la pestaña principal
                if (currentTabFilter === 'live' && rStatus !== 'live') show = false;
                // Si la pestaña actual es 'all' (Roadmap general), mostramos todo

                // Filtros de los selects
                if (area && area !== 'all' && rArea !== area) show = false;
                if (status && status !== 'all' && rStatus !== status) show = false;
                if (needsApi && !rApi) show = false;

                if (show) {
                    el.style.display = 'flex';
                    // Pequeña animación de entrada
                    el.style.opacity = '0';
                    setTimeout(() => el.style.opacity = '1', 50);
                    visibleCount++;
                } else {
                    el.style.display = 'none';
                }
            });

            if (emptyState) {
                emptyState.style.display = visibleCount === 0 ? 'block' : 'none';
            }
        };

        // Escuchar clics en las pestañas grandes
        tabs.forEach(tab => {
            tab.addEventListener('click', () => {
                // Quitar active de todas
                tabs.forEach(t => t.classList.remove('is-active'));
                // Poner active en la actual
                tab.classList.add('is-active');
                
                const target = (tab as HTMLElement).dataset.tabTarget || 'all';
                currentTabFilter = target;

                // Si hacemos clic en 'Lanzados', cambiamos el select de estado a 'live'
                if (target === 'live' && selectStatus) {
                    selectStatus.value = 'live';
                    selectStatus.disabled = true; // no permitir cambiarlo si la pestaña fuerza esto
                } else if (selectStatus) {
                    selectStatus.disabled = false;
                }

                filterRows();
            });
        });

        selectArea?.addEventListener('change', filterRows);
        selectStatus?.addEventListener('change', filterRows);
        toggleApi?.addEventListener('change', filterRows);

        if (btnClear) {
            btnClear.addEventListener('click', () => {
                if (selectArea) selectArea.value = 'all';
                if (selectStatus && !selectStatus.disabled) selectStatus.value = 'all';
                if (toggleApi) toggleApi.checked = false;
                filterRows();
            });
        }
    });
</script>
