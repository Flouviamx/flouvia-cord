---
// Ajustes › Facturas emitidas → /app/facturas.
//
// La bandeja dejó de ser una sub-pestaña de Ajustes cuando la factura pasó a
// ser un objeto de primera clase: ahora tiene filtros, saldo y acciones, y eso
// es una pantalla de trabajo, no una preferencia. Se conserva la ruta porque
// hay enlaces guardados y correos viejos que apuntan aquí.
export const prerender = false;
return Astro.redirect('/app/facturas', 301);
---
