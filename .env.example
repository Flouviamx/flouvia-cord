# ── Supabase (proyecto NUEVO, separado del de flouvia.com) ──
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=    # SSR only — nunca al browser

# ── Clerk (app NUEVA con signup ABIERTO — no invitation-only) ──
PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# ── Stripe Billing (suscripciones Free/$29/$59 + pago de cotizaciones) ──
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
PUBLIC_STRIPE_PUBLISHABLE_KEY=

# ── Emails transaccionales (Resend) ──
RESEND_API_KEY=

# ── CFDI — PAC de timbrado (mismo proveedor que la app de Shopify) ──
PAC_API_KEY=
